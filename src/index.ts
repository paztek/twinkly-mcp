#!/usr/bin/env node
/**
 * twinkly-mcp — entry point.
 *
 * Placeholder: the MCP server bootstrap (config parsing + transport wiring)
 * lands in Phase 3. For now this verifies the toolchain builds and runs.
 */
function main(): void {
  // eslint-disable-next-line no-console -- stderr is safe; stdout is reserved for the MCP stdio stream.
  console.error('twinkly-mcp: not yet implemented (see CLAUDE.md for the build plan)');
}

main();
